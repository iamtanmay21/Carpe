name: carpe_telecom_plugin
description: Native Telecom and alarm bridge for every Carpe Flutter engine.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.9.0
  flutter: ">=3.22.0"

flutter:
  plugin:
    platforms:
      android:
        package: com.local.carpe.telecom
        pluginClass: CarpeTelecomPlugin
