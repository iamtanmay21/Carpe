import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color background = Color(0xFF0F0F13); // Deep premium black-blue
  static const Color surface = Color(0xFF1C1C24); // Elevated dark surface
  static const Color surfaceVariant = Color(0xFF262630);
  static const Color surfaceElevated = Color(0xFF333342);

  static const Color accent = Color(0xFF6C5CE7); // Deep vibrant purple
  static const Color accentGradientStart = Color(0xFF8A2387); // Sunset gradient start
  static const Color accentGradientMid = Color(0xFFE94057); // Sunset gradient mid
  static const Color accentGradientEnd = Color(0xFFF27121); // Sunset gradient end

  // An alternative aura gradient for the microphone
  static const Color auraStart = Color(0xFF6366F1);
  static const Color auraEnd = Color(0xFFD946EF);

  static const Color text = Color(0xFFF8F9FA);
  static const Color textSecondary = Color(0xFFA1A1AA);
  static const Color textMuted = Color(0xFF71717A);

  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFF43F5E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color divider = Color(0xFF27272A);
}
